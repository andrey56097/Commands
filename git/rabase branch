// get last from master
update main branch

git checkout YOUR_BRANCH
git rebase master

git add -u // necessary

// fix conflicts and than
git rebase --continue 
git push origin YOUR_BRANCH -f




 git reset HEAD~1
